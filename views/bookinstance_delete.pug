extends layout 
block content 
   h1 #{title}: #{bookinstance.name} 
   p Do you really want to delete this book instance?
   form(method="POST", action="")
      div.form-group
         input#bookinstanceid.formcontrol(type="hidden", name="bookinstance", required="true", value=bookinstance._id)
         button.brn.btn-primary(type="submit") Delete